import { DEFAULT_IMAGE_PATH } from "../Common/path";

export const searchImage = `${DEFAULT_IMAGE_PATH}search.svg`;
export const heartImage = `${DEFAULT_IMAGE_PATH}heart.svg`;
export const homeImage = `${DEFAULT_IMAGE_PATH}home.svg`;
export const profileImage = `${DEFAULT_IMAGE_PATH}profile.svg`;
